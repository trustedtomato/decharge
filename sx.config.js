import { join } from 'path'

// paths
export const srcDir = 'src'
export const routesDir = join(srcDir, 'routes')
export const tempDir = '_sx/_temp'
export const tempRoutesDir = join(tempDir, routesDir)
export const distDir = 'dist'
